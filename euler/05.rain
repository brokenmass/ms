const MAX <- 20

var i <- 2

var multiple <- 1

while (i <= MAX) {
  var gcd <- multiple
  var other <- i

  while (other != 0) {
    var tmp <- other
    other <- gcd % other
    gcd <- tmp
  }

  multiple <- multiple * i / gcd

  i <- i + 1
}

print("multiple: ")
print(multiple)