var mem =malloc(100)

var pos =mem
writeByte(pos, 'a')

pos =+(pos, 1)
writeByte(pos, 'b')

pos =+(pos, 1)
writeByte(pos, 'c')


pos = mem
print(readByte(pos))
print('\n')

pos = +(pos, 1)
print(readByte(pos))
print('\n')

pos = +(pos, 1)
print(readByte(pos))


